# Database connection (PostgreSQL)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/taskforge?schema=public"

# Next Auth Configuration
# RUN `npx auth secret` or `openssl rand -base64 32` to generate a secret in production
NEXTAUTH_SECRET=""
NEXTAUTH_URL="http://localhost:3000"

# AI API Key (Anthropic Claude - Opção 1)
ANTHROPIC_API_KEY=""
ANTHROPIC_MODEL="claude-sonnet-4-20250514"

# AI API Key (Google Gemini - Opção 2 Fallback)
GEMINI_API_KEY=""
GEMINI_MODEL="gemini-2.5-flash"
