# Database connection (PostgreSQL local)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/taskforge?schema=public"

# Next Auth Configuration
# RUN `npx auth secret` or `openssl rand -base64 32` to generate a a secret in production
NEXTAUTH_SECRET=""
NEXTAUTH_URL="http://localhost:3000"

# AI API Key (Anthropic Claude 3.5 Sonnet)
ANTHROPIC_API_KEY=""
